## Git Commits

- **NEVER** add a commit signature (Co-Authored-By, Generated with) unless **explicitly requested**
- Default behavior: commits should have NO signature footer
- Signature is only added if user explicitly says "commit with your signature" or similar
